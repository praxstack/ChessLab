## 1. Runtime and legal engine
- [x] 1.1 CP-001/CP-002 Install the web runtime dependencies and native Stockfish; verify real UCI bestmove, legal history replay, bounded search and malformed-input checks.
- [x] 1.2 CP-004 Implement persistent accounts, ownership-scoped games, sessions and request validation; verify authentication, isolation, stale revision rejection and server restart persistence with runnable tests.

## 2. Working coach platform
- [x] 2.1 CP-001/CP-005 Build the recorded-layout bot game UI and functional settings; verify both colors, legal move entry, promotion, bot reply, resignation and narrow viewport operation.
- [x] 2.2 CP-002/CP-003 Build review, PGN import/export, per-position explanations, candidate lines and nested variations; verify stale-response rejection, invalid import preservation, saved branch return and reload.
- [x] 2.3 CP-006 Provide original introductory lessons and puzzles; validate every position/solution and verify wrong answers do not award progress.

## 3. Delivery
- [x] 3.1 CP-001 through CP-006 Run application, server and browser checks on the integrated build; record actual results and unresolved limits without counting report/setup checks as product behavior.
- [x] 3.2 CP-007 Document startup, dependencies, architecture and staged platform scope; commit and push to a private GitHub repository, then verify privacy and remote HEAD.

import React from 'react';
import {markColorNames} from '../../shared/board-marks.js';
export default function BoardDrawingTools({tool,color,marks,disabled,onTool,onColor,onClear}){
 return <section className="board-drawing-tools" aria-label="Board drawing tools"><div className="drawing-tool-row"><div className="drawing-modes" role="group" aria-label="Drawing mode">{[['move','Move pieces'],['arrow','Draw arrow'],['square','Highlight square']].map(([value,label])=><button type="button" key={value} aria-pressed={tool===value} disabled={disabled} onClick={()=>onTool(value)}>{label}</button>)}</div><label className="drawing-color">Color<select aria-label="Drawing color" value={color} disabled={disabled} onChange={e=>onColor(e.target.value)}>{Object.entries(markColorNames).map(([key,name])=><option key={key} value={key}>{name}</option>)}</select></label><button className="text-button" disabled={disabled||!marks.length} onClick={onClear}>Clear marks</button></div><p>{tool==='arrow'?'Choose the starting square, then the ending square.':tool==='square'?'Choose a square to highlight it.':'Right-drag for an arrow. Right-click for a square.'} <span>{marks.length} / 64 marks</span></p><details><summary>Colors and saved marks</summary><p>Right-drag: yellow; Ctrl red, Alt blue, Shift green. Right-click: red; Ctrl yellow, Alt blue, Shift green. Repeat a mark to remove it. Marks stay with this study position until you clear them.</p>{marks.length>0&&<ul aria-label="Drawings at this position">{marks.map(m=><li key={m.from+m.to}>{markColorNames[m.color]} {m.from===m.to?`highlight on ${m.from}`:`arrow ${m.from} to ${m.to}`}</li>)}</ul>}</details></section>;
}

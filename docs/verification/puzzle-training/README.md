# Local custom puzzle training evidence

Puzzles now opens 6,100,952 locally stored source puzzles across 73 themes. Custom practice supports source rating/theme filters, server-owned multi-move attempts, hints, reveal, skip, recent history, retrying mistakes, solution playback and native analysis in a separate saved study. The original six starter exercises remain accessible.

[Video](proof/local-user-flow.webm) · [Receipt](proof/receipt.json) · [Catalogue](proof/22-training-catalogue.png) · [Restored attempt](proof/23-persisted-training.png) · [Native analysis](proof/24-puzzle-native-analysis.png) · [Mobile](proof/25-mobile-training.png)

The complete 304,429,328-byte [CC0 source export](https://database.lichess.org/#puzzles) and 2,764,046,336-byte indexed catalogue are present under ignored data/puzzles. [Provenance](../../../references/puzzles/provenance.json) and [local data hashes](local-data.json) identify this installation. Import validates every row's structure, keeps a bounded buffer, checks SQLite integrity and refuses overwrite. Each selected puzzle legally replays its trigger and complete solution before serving. Root also legally replayed a sample from every theme. These checks do not constitute a fresh engine evaluation of all source puzzles.

All 71 application tests pass, preserving the prior 68 tests. The importer and SQLite snapshot checks pass. New checks cover answer secrecy, source trigger, source legality, alternate immediate mates, revisions, account isolation, wrong moves, hint reset, one active attempt per account, duplicate completion refusal, assistance-aware totals, restart, mistake retry, missing/empty source, skip and saved study history/result. Strict OpenSpec validation passes ten changes. The production-entrypoint doctor passes.

The final 38-flow recording uses native Stockfish 19, the actual full local catalogue, synthetic accounts and temporary account SQLite. It preserves all 32 previous flows. Added flows choose a source theme/rating range, make a legal wrong move, use a hint, submit the correct move and receive its reply, reload and recover the attempt, finish, analyze, retain the previous opening study, retry mistakes, reveal, navigate and flip on mobile, retry without help and check totals. No unexpected external resource/model requests or browser errors occurred. The 49.16-second video and screenshots were inspected. The harness closes its own browser, server and engines and removes its temporary account database.

An expected-red test exposed a stale hint after a correct move; the hint now clears as the position advances. The first browser attempt failed on a missing rules-library import in the harness. The second passed, but visual review found missing terminal result metadata in puzzle studies. The final build records the actual result and labels imported-game winners by color. Failure/intermediate logs are retained; earlier recordings remain locally in ignored verification history. No assertion was weakened.

The [official puzzle guide](https://support.chess.com/en/articles/8608686-how-do-puzzles-work-on-chess-com) and [selection guide](https://support.chess.com/en/articles/8708983-how-are-the-next-puzzles-chosen-for-me-when-i-am-training) informed custom practice, filter and retry behavior. Source puzzle ratings are not calibrated user chess ratings. This delivery does not implement rated adaptation, daily puzzles, rush, battle, the complete curriculum or the conversational branching tutor. Full platform parity remains active.

Root performed source, screenshot and video review. A fresh Astra Team check still rejects the host runtime; independent acceptance is unavailable. just check still fails on installed unslop skill-manifest drift. No gate or governing policy changed. The control-app map was updated in a bounded maintenance pass. The manifest binds final application source, assets, build, tests and proof; older feature manifests remain historical.

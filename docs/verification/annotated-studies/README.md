# Annotated studies evidence

PGN import/export now carries legal nested alternatives, comments and numeric move annotations. Position notes attaches learner text and symbols to a branch identity and exact ply, including the original game and its starting position. Existing branch questions remain intact. Export saves pending edits first; a failed/stale save blocks the download and preserves the draft. Undo refuses to remove annotated original-game history.

[Video](proof/local-user-flow.webm) · [Receipt](proof/receipt.json) · [Position notes](proof/44-position-notes.png) · [Mobile](proof/45-mobile-notes.png) · [Restored study](proof/46-restored-study.png) · [Protected unsaved draft](proof/47-protected-note-draft.png)

The official [comments and variations guide](https://support.chess.com/en/articles/8648793-how-can-i-add-comments-and-variations-to-games) and [PGN export guide](https://support.chess.com/en/articles/8705305-how-do-i-get-a-pgn-of-my-game), read 2026-09-13, informed this workflow. No vendor instructional content or user games are included.

The installed chess.js 1.4.0 generated parser is reused and now pinned exactly. Every imported mainline and alternative is replayed with chess.js; illegal or excessive structures fail before insertion. The existing study validator was moved into the format module and extended with unique branch/ply annotations. Account ownership, saved-study revisions, original-prefix checks and branch-cycle checks remain enforced. Only the import/study-save endpoints accept up to 1 MB of JSON; ordinary game/account request limits stay at 256 KB. File and tree limits are documented in the local workspace guide.

A versioned .chesslab.json file preserves the original moves, starting FEN, title, headers, complete tree, exact question/comment text, glyphs, selected branch and return anchor. Import creates a separate owned study, excluding account identifiers, running clocks and engine caches. PGN is the interoperability format: identities, empty branches, root comments and braces are normalized. A ChessLabOriginalPly tag preserves the original-game boundary when a terminal continuation must be emitted as a PGN main line. An application that ignores this custom tag sees the continued line; use the study file for exact ChessLab fidelity.

All 87 application tests pass, retaining the previous 82. New checks cover nested PGN round trips, illegal alternatives, Black-to-move FEN numbering, exact arbitrary note text, portable-field exclusions, invalid tree/annotation rejection, ownership, stale revisions, restart, original-game preservation and annotated-history undo protection. The original numbering, zero-move FEN round-trip and invalid-FEN HTTP checks initially exposed regressions; the implementation was repaired without weakening those protected tests. The preserved invalid-FEN failure log is under ignored .scratch/annotated-studies. Initial focused checks are labeled separately from the final full-suite evidence. Snapshot/importer checks, production-entrypoint checks and strict validation of all fifteen OpenSpec changes pass.

The production recording covers 66 flows with 48 screenshots. It preserves all previous 60 flows and adds position notes/glyphs, nested annotated PGN, actual downloaded files, save-before-export, exact reload/mobile study restoration, a real stale-save rejection and annotated re-import. The isolated synthetic account explicitly induces one 409 at the current study endpoint, asserts that exact failure and the intact draft/no-download behavior, then requires zero unrelated HTTP failures. No browser engine/model downloads or unexpected external resources occurred. The harness cleans up its browser, server, engines and temporary database/files. Root inspected desktop/mobile notes, the protected draft and video at 70 seconds.

Independent team acceptance remains unavailable because the installed Astra validator rejected this host at 06:04 UTC. The pre-existing unslop skill-manifest drift still fails just check. Exact outputs are retained and no governing gate was changed. This is learner-authored annotation and portable local analysis; free-form model coaching, remaining platform parity, the original full branching tutor, later human multiplayer and billing remain active work.

The first recording passed functionally. Final review added portable bot/player names, corrected singular variation labels and clarified that a note belongs to its position without implying an unsaved draft is already stored. That first recording is retained under ignored .scratch/annotated-studies/first-proof. Only the final candidate is bound by the manifest.

The final boundary review also rejected empty annotation branch identifiers, which previously could leave a note invisible. A focused assertion covers that malformed-file case. The intermediate reviewed recording remains in .scratch/annotated-studies/review-proof, and the full checks and recording were repeated against the corrected source.

The accepted final recording is 73.56 seconds; all 66 flows passed with 48 screenshots. Root inspected the final mobile notes and video at 70 seconds. The manifest binds 542 source/build/specification/check/proof files and the installed parser hash.

# Local Puzzle Rush evidence

Puzzle Rush now provides three-minute, five-minute and untimed Survival runs. Each full solution scores one point; the first legal mistake or skip fails that puzzle once. Three failures end the run. The server owns a fixed deadline, revisions and account state. Reload and restart preserve the position and score. Local bests are separated by mode; finished runs retain every encountered puzzle for separate Custom practice and native analysis. Rated profiles and Daily credit remain unchanged.

[Video](proof/local-user-flow.webm) · [Receipt](proof/receipt.json) · [Survival](proof/29-rush-survival.png) · [Results](proof/30-rush-results.png) · [Mobile clock](proof/33-mobile-rush-clock.png)

The [official puzzle guide](https://support.chess.com/en/articles/8608686-how-do-puzzles-work-on-chess-com), read 2026-09-13, informed timed/Survival modes, three failures, personal scores and post-run review. Local difficulty begins near source rating 400 and increases by encounter, with nondecreasing ratings and no repeats within the run. It uses the existing covering index; a fresh direct check against the full catalogue took 14 ms to start and 15 ms to advance on this Mac. This is a measured sample, not a performance guarantee or proprietary sequencing equivalence.

All 77 application tests pass, protecting the previous 74. Focused checks cover once-only full-line credit, legal/illegal moves, hidden solutions, stale requests, account isolation, mutually exclusive training, exact deadlines in both timed modes after restart, untimed recovery, failed-save rollback, source exhaustion and unchanged original runs after retries. The HTTP path also verifies multi-move recovery and isolated access. SQLite snapshot/importer checks, the production-entrypoint doctor and strict validation of twelve OpenSpec changes pass.

The final 48-flow production-build recording preserves all earlier 43 flows and adds Survival board input, automatic advance, reload recovery, three-strike results, saved personal best, separate retry, timed clock, mobile progress and saved-run history. It uses synthetic accounts in temporary SQLite, the full local 6,100,952-puzzle catalogue and native Stockfish 19. No unexpected external resource or model requests occurred. The harness cleans up its browser, server, engine and account database while retaining proof.

Root inspected desktop/mobile screenshots and sampled the 52.64-second video. An earlier passing recording exposed a mobile layout issue: the clock sat below the board. The accepted build places a compact score/time/lives strip above the mobile board. That earlier recording remains under ignored `.scratch/puzzle-rush/first-proof/` as intermediate evidence. The final recording is the only accepted proof in this folder. No dependencies were added.

Root reviewed the source and performed bounded control-app map maintenance. Independent team acceptance remains unavailable: the installed Astra validator rejected this host again at 04:35 UTC. `just check` still fails on installed unslop skill-manifest drift; its exact output is preserved under checks. No governing gate or policy was changed. The separate manifest binds source/build/proof and references the unchanged local catalogue. Full platform parity, broader curriculum, the full conversational branching tutor, later human multiplayer/Battle and billing remain active work.

# Guided learning evidence

Learn now opens a course path grouped into four levels. Twelve original guided lessons contain 24 legally replayed board challenges; the six original lessons remain accessible with their IDs, answers and progress. The lesson library searches titles, descriptions, teaching terms and body text, with level and topic filters. Next unfinished lesson and per-course completion come from account progress.

[Video](proof/local-user-flow.webm) · [Receipt](proof/receipt.json) · [Path](proof/34-learning-path.png) · [Library](proof/36-lesson-library.png) · [Mobile promotion](proof/37-mobile-lesson.png) · [Completed course](proof/39-course-complete.png)

The [official Lessons guide](https://support.chess.com/en/articles/8609703-how-do-lessons-work-on-chess-com), read 2026-09-13, informed the path, library and continued-lesson workflow. Instruction here is original. Six advanced CC0 examples retain source IDs, FEN, trigger, line, rating and game attribution under references/puzzles/curriculum-lines.json. The remaining challenges are authored teaching positions. This does not reproduce the vendor’s complete lesson catalogue or establish learning outcomes.

The server stores a session identity, step, accepted moves and revision for each account/lesson. Wrong and illegal moves cannot advance the board. Completion is inserted only after both challenges are solved, in the same transaction as the saved session. A failed save rolls both back. Reload/restart resumes the current challenge; a deliberate repeat creates a new identity while preserving completion. The old single-answer endpoint cannot complete a new guided lesson. The existing legal puzzle action engine and Board are reused; no dependencies were added.

All 81 application tests pass, protecting the earlier 77, together with SQLite snapshot/importer checks. Content checks replay all 24 full challenge lines, prove final mates and verify that c8=Q stalemates while c8=R does not in the promotion example. State/HTTP checks cover wrong input, multi-move partial credit, all-challenge completion, restart, ownership, stale identities, repeated lessons and rollback after a storage failure. Focused content checks passed again after search metadata and instructional notation were corrected. Strict validation passes thirteen OpenSpec changes, and the production-entrypoint doctor passes.

The final 54-flow production-build recording preserves all previous 48 flows and adds guided path entry, second-challenge reload recovery, all three lessons in the first course, course completion, next lesson, filtered search, queen/rook promotion at 390px, and two advanced multi-move mates. Saved starter progress remains present. It uses synthetic accounts in temporary SQLite and the native Stockfish 19/local puzzle catalogue for the protected earlier flows. No unexpected external resource or model requests occurred. The harness cleans up its browser, server, engine and temporary account database while retaining proof.

Root inspected desktop/mobile screenshots and sampled the 60.92-second video. The first recording failed because “promotion” did not match “Promote with a plan.” Search now includes instructional keywords and body text. The failed recording and log remain under ignored .scratch/learning-path/first-proof. Only the final recording is accepted here. Completed-board labels and rook capture notation were also corrected before the final build.

Root reviewed the code and performed bounded control-app map maintenance. Independent team acceptance remains unavailable: the installed Astra validator rejected the host at 05:16 UTC. The existing installed unslop skill-manifest drift still fails just check; its exact output is preserved. No governing gate or policy changed. The separate manifest binds source/build/proof and the unchanged catalogue identity. Full platform parity, further curriculum, the full branching conversational tutor, later human multiplayer and billing remain active work.

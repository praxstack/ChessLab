# Persistent board drawing evidence

Review and Analysis now save arrows and square highlights with an exact study branch and ply. Four colors, right-drag/right-click modifiers, keyboard controls and touch tools share the existing board renderer and study-note storage. Repeating a drawing removes it; changing its color replaces it. Clear marks preserves the current comment and glyphs, other positions and original game moves. Pending gestures cancel on navigation, orientation changes, pointer cancellation/lost capture or Escape within the board.

[Video](proof/local-user-flow.webm) · [Receipt](proof/receipt.json) · [Desktop drawings](proof/48-colored-board-drawings.png) · [Mobile touch tools](proof/49-mobile-drawing-tools.png) · [Restored drawings](proof/50-restored-board-drawings.png)

The official [drawing guide](https://support.chess.com/en/articles/8568781-how-can-i-draw-arrows-on-the-board-like-streamers-do), read 2026-09-13, informed the mouse workflow and color modifiers. These local study drawings intentionally persist until cleared and also support touch/keyboard controls. They are learner annotations, not engine recommendations or assertions of legal moves. No new dependency or vendor instructional content was added.

The shared validator accepts at most 64 unique geometries per node, valid board squares and four scalar color codes. Invalid drawings fail before saving or importing. Existing account ownership, study revisions and tree limits remain enforced. Standard PGN cal/csl directives carry drawings alongside text; exact study files preserve tree identities and all marks. PGN retains its documented normalization of branch-root text and empty alternatives; use the study file for exact fidelity.

The first browser run exposed a null drawing-state error in the shared position-editor board. The guard was fixed at that shared renderer; no protected test was removed. The subsequent complete 72-flow run passed and is retained under ignored .scratch/board-annotations/review-proof. Root inspected its desktop and mobile screenshots. Final review added Escape/lost-capture cancellation and corrected the active-tool text color to use the existing palette. Only the final production recording and hashes are accepted below.

Independent team acceptance remains unavailable: the installed Astra validator rejected the host at 06:37 UTC. The pre-existing installed unslop skill-manifest drift still fails just check. Exact outputs are retained; no governing gate or policy changed. Full platform parity, the original branching conversational tutor, later human multiplayer and billing remain active work. This increment establishes local drawing functionality only.

The final serial run passed all 89 application tests, retaining the preceding 87, plus SQLite snapshot/importer checks and the actual production entrypoint. All sixteen OpenSpec changes pass strict validation. The recording passed 72 flows, retaining the preceding 66, with 51 screenshots and an 89.92-second video (10,836,569 bytes). It exercises mouse modifiers, toggle/recolor, knight geometry, flipped coordinates, cancellation, keyboard and actual touch events, nested-branch reload, comment preservation, downloaded PGN/JSON restoration and clear-only-current-node behavior. Root inspected the final desktop/mobile screenshots and video at 84 seconds. All accounts/files are synthetic; the harness removes its temporary database, browser, server and native-engine processes. No unexpected external resource/model requests, JavaScript errors or unrelated failing HTTP responses occurred.

Installed locally on 2026-09-13 at 06:48 UTC from source commit `88be6a06481f07e87a41c38b0d25a4fc9c33fdae`. All 548 manifest files and the pinned parser matched after rebuild; the full local puzzle catalogue hash was unchanged. Served JS/CSS matched byte for byte, Stockfish 19 was ready, and guest study-file access returned 401. Fresh local browser inspection showed the loaded 166-bot/10-engine picker and a working shared position editor. SQLite integrity passed; the two accounts, two games, zero progress and empty collection tables were preserved. The consistent backup is `data/backups/before-board-annotations-20260913.sqlite`. [PR #12](https://github.com/praxstack/ChessLab/pull/12) is open against the annotated-studies branch.

The clean-checkout guard caught two unrelated design-log deletions during installation (`design/imagine-refinements-run.txt` and `design/imagine-refinements.txt`). Both branches contain identical versions of those files. The deletions were preserved without staging or restoring them; original main also retains that pre-existing state. This documentation-only installation receipt and checklist do not change the attested application build.

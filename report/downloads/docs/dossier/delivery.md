## Update: 7 September 2026

A bot/coach application has since been implemented, and the original archive was committed as `993a24e`. See the [current application chapter](application.md) for working behavior and verification. The following sections document the earlier setup and HTML-report runs, including failures that occurred then.

## Historical record

## The short status

The earlier run completed repository preparation and planning. This run creates the HTML research dossier. Neither run implements the chess tutor. No customer outcomes, revenue, deployment or application end-to-end results are established.

{{EVIDENCE}}

## Earlier setup run: what was actually done

| Work | Result | Limit |
| --- | --- | --- |
| Read supplied research | Both pasted reports, the PDF and referenced conversation reviewed | Source claims are not all independently verified |
| Recover the motivating example | Original two lesson screenshots inspected | No full legal puzzle/engine evaluation was certified |
| Assess competitors | Selected official product pages checked | Documentation review, not a full hands-on comparison |
| Inspect project | Confirmed ChessLab began empty | No inherited chess application to reuse |
| Initialize Git | Created `main` | Initial commit failed; no remote created |
| Install project skill links | 180 per-host links plus two mirrors, backed by a 144-entry source manifest | Existing global packs reused; no broad package installer run |
| Configure Matt workflow | Local issue guidance, five triage states and one domain context | Scratch issues are local; OpenSpec is the single specification source |
| Configure OpenSpec | Initialized the project and linked 12 existing skills | Global skill sources/settings not overwritten |
| Draft the first change | Proposal, design, six requirements and nine unchecked tasks | Draft recommendations, no application implementation |
| Preserve research | Two original texts and PDF copied with SHA-256 provenance | No redistribution license asserted for third-party materials |
| Verify setup | Link checks, helper self-test, strict OpenSpec validation, Gstack help and Clay router check | No full skill/model benchmark or product browser test |
| Fix setup portability | Made the two project directory mirrors relative | Global skill installations are still machine prerequisites |

Two bounded agents helped with research assessment and skill inventory in that setup run. Their findings were synthesized into the project documents and checked against observable files. This report run uses no new subagents.

## The skill packs, explained

| Pack | Entries in each primary host tree | Purpose |
| --- | ---: | --- |
| Matt Pocock | 22 | Problem definition, domain understanding, specification/ticket workflows, implementation and review |
| Pstack Unslop and Clay Unslop | 2 | Clear project prose; separate Clay audit/rewrite workflows |
| OpenSpec | 12 | Explore, propose, create, continue, apply, update, fast-forward, sync, archive, bulk archive, verify and onboard |
| Gstack | 54 | 53 functional entries plus runtime/router for product/engineering review and related workflows |

Codex and Claude use different Gstack variants. Cursor mirrors the Claude tree. There are 90 entries per primary host tree; summing them does not mean 180 distinct capabilities. The library includes the exact manifest and setup helper. Global skill bodies and runtime dependencies remain outside this portable dossier.

Checks verify entrypoint hashes, not every byte of each dependency tree. Gstack runtime aliases repeat two identities recursively; top-level names are unique, and host-loader deduplication has not been tested. Full model-based skill quality was not evaluated.

## This HTML report run

This run rechecks local setup, captures the available original conversation and its two images, revisits selected official product/pricing pages, and adds an investor-oriented interpretation. It builds a navigable reading site with a complete document library, original downloads, a conditional exchange illustration, an economics calculator and printable pages.

The HTML is derived from Markdown, source files and shared presentation assets. A source/output hash manifest catches stale generated files. Local links and interactive behavior are checked separately. The final report verification receipt is linked below; it is evidence for this reading site only.

{{REPORT_RECEIPT}}

## Git and release truth

At the start of this run, the 26 earlier setup files were still staged and the repository had no commits. The earlier commit command failed because the configured SSH signing key could not be unlocked. An SSH agent connection was unavailable. Signing was not disabled and no credential files were changed.

This run preserves that staged work and adds the dossier in the working tree. It does not claim a commit, push, public website or released chess app. An authorized user can unlock the signing key and complete the baseline later. The report is usable locally without that commit.

## What remains to build or decide

- Accept or revise the first product scope and select concrete application dependencies.
- Reconstruct and educator-review a legal position corpus.
- Implement PGN import, legal state, branching, questions, comparison, persistence, export and accessible controls.
- Test actual explanation correctness and learning transfer with people.
- Add the original adjustable-AI play loop when the exploration interaction is useful.
- Establish repeat use, pricing, serving/support costs and a reachable distribution channel.
- Decide licensing, hosting, ownership and release responsibilities before public distribution.

The existing `just e2e` explicitly reports that no product end-to-end test exists and exits 1. The report's browser checks do not replace it. All nine product tasks remain unchecked.


const D = window.CHESSLAB_DATA;
const app = document.getElementById('app');
const state = {route:'overview',domain:'All',priority:'All',query:'',selectedMove:6,selectedSquare:null,mode:'Coach',messages:[{who:'ai',text:'The position is balanced, but your last move weakened the dark squares. What reply were you expecting?'},{who:'user',text:'I thought the knight could not jump because the pawn blocks it.'},{who:'ai',text:'That is the key misunderstanding. Knights jump over occupied squares. Test 7...Nxe4 in a new branch, then compare material and king safety.'}]};
const icons={overview:'◫',map:'⌘',prototype:'♟',review:'⌁',learning:'✦',community:'◎',brd:'▤',screens:'▦',architecture:'⬡',research:'⌕'};
const routes=[['overview','Overview'],['map','Capability map'],['prototype','Play prototype'],['review','Branch tutor'],['learning','Learning'],['community','Community'],['brd','BRD matrix'],['screens','Screen atlas'],['architecture','Architecture'],['research','Research']];
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));}
function shell(content){return `<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">CL</div><div class="brand-copy"><h1>ChessLab Atlas</h1><small>Clean-room product dossier</small></div></div><nav class="nav">${routes.map(([r,l])=>`<button data-route="${r}" class="${state.route===r?'active':''}"><span class="icon">${icons[r]}</span><span class="nav-label">${l}</span></button>`).join('')}</nav><div class="sidebar-foot"><strong>Research boundary</strong><p>Capability mapping only. Original ChessLab UX, identity, copy and assets.</p></div></aside><main class="main"><header class="topbar"><div class="crumb">ChessLab / <b>${routes.find(x=>x[0]===state.route)?.[1]}</b></div><div class="top-actions"><button class="btn ghost hide-mobile" data-route="research">${D.meta.sourceCount} sources</button><a class="btn primary" href="brd-print.html" target="_blank">Open full BRD</a></div></header><div class="content">${content}</div></main><nav class="mobile-nav">${[['overview','⌂','Home'],['prototype','♟','Play'],['review','⌁','Review'],['learning','✦','Learn'],['map','⌘','Map']].map(([r,i,l])=>`<button data-route="${r}"><span>${i}</span>${l}</button>`).join('')}</nav></div>`}
function tag(t,cls=''){return `<span class="tag ${cls}">${esc(t)}</span>`}
function overview(){const domains=[...new Set(D.capabilities.map(x=>x.domain))];return `<section class="hero"><div class="eyebrow"><span class="dot"></span> Research-backed product operating system</div><h2>Map the whole board.<br>Teach the move beneath it.</h2><p>ChessLab Atlas maps the complete online-chess platform surface, then turns analysis into a persistent conversation with the exact position. It is a clean-room specification, original prototype and implementation compass in one place.</p><div class="hero-actions"><button class="btn primary" data-route="map">Explore the map</button><button class="btn" data-route="review">Try branch tutor</button><button class="btn" data-route="brd">Open requirements</button></div><div class="stat-grid"><div class="stat"><b>${D.meta.capabilityCount}</b><span>mapped capabilities</span></div><div class="stat"><b>${D.meta.screenCount}</b><span>screens and states</span></div><div class="stat"><b>${D.meta.requirementCount}</b><span>functional + NFR rows</span></div><div class="stat"><b>${D.meta.sourceCount}</b><span>first-party and project sources</span></div></div></section><section class="section"><div class="section-head"><div><h3>The strategic shape</h3><p>Parity provides table stakes. Branch-native learning provides the reason to exist.</p></div></div><div class="grid three"><div class="card"><div class="eyebrow">01 · Platform</div><h4>Play, learn, analyze, belong</h4><p>Human chess, Daily games, bots, coach, review, puzzles, lessons, stats, events, clubs, settings and trust systems form one connected platform.</p><div class="tag-row">${tag('Capability parity','proto')}${tag('Original UX')}</div></div><div class="card"><div class="eyebrow">02 · Differentiator</div><h4>A durable thinking workspace</h4><p>Questions, predictions, branches, explanations, evidence and return anchors remain attached to the exact node, even inside nested alternatives.</p><div class="tag-row">${tag('Position-bound dialogue','proto')}${tag('Nested branches')}</div></div><div class="card"><div class="eyebrow">03 · Invariant</div><h4>Fair Play Lock</h4><p>Engine analysis and coaching are disabled for ongoing human games. Imported completed games, studies, bots and unrated learning are the safe learning surface.</p><div class="tag-row">${tag('P0 trust','p0')}${tag('No live overlay')}</div></div></div></section><section class="section"><div class="section-head"><div><h3>${domains.length} product domains</h3><p>Each domain expands into capabilities, requirements, screens, services and measurable acceptance.</p></div><button class="btn" data-route="map">View all</button></div><div class="grid four">${domains.slice(0,8).map((d,i)=>{const cs=D.capabilities.filter(x=>x.domain===d);return `<article class="card domain-card" style="--accent:${['rgba(110,219,209,.15)','rgba(246,198,91,.15)','rgba(183,165,255,.15)','rgba(255,129,116,.15)'][i%4]}"><b class="big">${cs.length}</b><h4>${esc(d)}</h4><p>${esc(cs[0].requirement)}</p><div class="tag-row">${tag(`${cs.filter(x=>x.priority==='P0').length} P0`,'p0')}${tag(`${cs.filter(x=>x.status==='Prototype').length} prototyped`,'proto')}</div></article>`}).join('')}</div></section><section class="section"><div class="grid two"><div class="card"><div class="section-head"><div><h3>Founder decisions</h3><p>Decisions this BRD makes explicit.</p></div></div><ol style="color:var(--muted);line-height:1.8;padding-left:20px"><li>Build a clean-room capability equivalent, not a visual clone.</li><li>Make the branch-native review loop the first differentiated product.</li><li>Keep legal play and saved state authoritative before adding AI prose.</li><li>Block live human-game assistance by design, not by warning text.</li><li>Measure teach-back and delayed transfer, not only engagement.</li></ol></div><div class="card"><div class="section-head"><div><h3>Current evidence boundary</h3><p>The repo has a working coach-stage prototype, not full platform parity.</p></div></div><div class="callout">The Atlas distinguishes mapped, prototyped and shipped behavior. A screenshot proves rendering, not chess correctness, model quality, security, scale, or learning outcomes.</div></div></div></section>`}
function mapView(){const domains=['All',...new Set(D.capabilities.map(x=>x.domain))];const selected=state.domain==='All'?D.capabilities:D.capabilities.filter(x=>x.domain===state.domain);return `<section class="hero"><div class="eyebrow">One-to-one capability atlas</div><h2 style="font-size:52px">Reference breadth, original implementation.</h2><p>Every row translates a publicly documented product capability into a clean-room ChessLab requirement. Select a domain to inspect the mapping.</p></section><section class="section map-layout"><div class="map-list">${domains.map(d=>`<button data-domain="${esc(d)}" class="${state.domain===d?'active':''}"><strong>${esc(d)}</strong><br><small>${d==='All'?D.capabilities.length:D.capabilities.filter(x=>x.domain===d).length} capabilities</small></button>`).join('')}</div><div><div class="section-head"><div><h3>${esc(state.domain)}</h3><p>${selected.length} mapped capabilities</p></div><a class="btn" href="research/one-to-one-traceability.csv" download>Download traceability</a></div><div class="cap-list">${selected.map(c=>`<article class="cap-item"><div>${tag(c.id)}${tag(c.priority,c.priority.toLowerCase())}${c.status==='Prototype'?tag('Prototype','proto'):''}</div><h5>${esc(c.capability)}</h5><p>${esc(c.requirement)}</p><div class="tag-row">${c.sources.map(s=>tag(s)).join('')}</div></article>`).join('')}</div></div></section>`}
const startPieces={'a8':'♜','b8':'♞','c8':'♝','d8':'♛','e8':'♚','f8':'♝','g8':'♞','h8':'♜','a7':'♟','b7':'♟','c7':'♟','d7':'♟','e7':'♟','f7':'♟','g7':'♟','h7':'♟','a2':'♙','b2':'♙','c2':'♙','d2':'♙','e2':'♙','f2':'♙','g2':'♙','h2':'♙','a1':'♖','b1':'♘','c1':'♗','d1':'♕','e1':'♔','f1':'♗','g1':'♘','h1':'♖'};
let pieces={...startPieces};
function board(){let out='';const files='abcdefgh';for(let r=8;r>=1;r--){for(let f=0;f<8;f++){const sq=files[f]+r;const light=(f+r)%2===1;out+=`<button class="sq ${light?'light':'dark'} ${state.selectedSquare===sq?'selected':''}" data-square="${sq}" aria-label="${sq} ${pieces[sq]||'empty'}"><span class="piece">${pieces[sq]||''}</span></button>`}}return out}
function playPrototype(){return `<section class="section" style="margin-top:0"><div class="section-head"><div><div class="eyebrow">Interactive clean-room prototype</div><h3>Play workspace</h3><p>Choose a mode, start a session, move pieces, and inspect the connected coaching rail.</p></div><div class="signal"><span class="dot"></span> Demo state, not a legal engine</div></div><div class="prototype-shell"><div class="board-panel"><div class="game-head"><div class="player"><div class="avatar">CL</div><div><strong>Atlas Coach</strong><br><small style="color:var(--muted)">Guided practice · 1200</small></div></div><div class="clock">09:42</div></div><div class="board-wrap"><div class="eval"><span>+0.4</span></div><div class="chessboard">${board()}</div></div><div class="game-head" style="margin:12px 0 0"><div class="player"><div class="avatar" style="background:linear-gradient(135deg,var(--amber),var(--coral))">P</div><div><strong>Prax</strong><br><small style="color:var(--muted)">Learning game</small></div></div><div class="clock">09:56</div></div></div><aside class="rail"><div class="tabs"><button class="active">Coach</button><button>Moves</button><button>Settings</button></div><div class="mode-grid">${[['Live','Human matchmaking'],['Bot','Fixed or adaptive'],['Coach','Move feedback']].map(([m,d])=>`<button class="mode ${state.mode===m?'active':''}" data-mode="${m}"><b>${m}</b><small>${d}</small></button>`).join('')}</div><div style="height:12px"></div><div class="coach-bubble"><strong>Think before reveal</strong><br>What is your opponent threatening, and which of your pieces is doing the least work?</div><div class="section" style="margin-top:14px"><h4 style="margin:0 0 10px">Game controls</h4><div class="grid two"><button class="btn">Hint 1</button><button class="btn">Undo</button><button class="btn">Offer draw</button><button class="btn">Resign</button></div></div><div class="section" style="margin-top:14px"><h4 style="margin:0 0 10px">Session contract</h4><p style="font-size:12px;color:var(--muted);line-height:1.55">Guided games are unrated, resumable and private by default. In human games, coaching and engine controls disappear under the Fair Play Lock.</p></div></aside></div></section>`}
const moves=['e4','c5','Nf3','d6','d4','cxd4','Nxd4','Nf6','Nc3','a6','Be3','e5','Nf3','Be7','Bc4'];
function reviewView(){return `<section class="section" style="margin-top:0"><div class="section-head"><div><div class="eyebrow">ChessLab differentiation</div><h3>Branch-native review studio</h3><p>The actual game is immutable. Exploration can nest forever and still return home.</p></div>${tag('Fair Play: completed game','proto')}</div><div class="prototype-shell"><div class="board-panel"><div class="game-head"><div><strong>Sicilian study</strong><br><small style="color:var(--muted)">Actual line · node 7 · White to move</small></div><div>${tag('+0.35')}${tag('Depth 18')}</div></div><div class="board-wrap"><div class="eval"><span>+0.35</span></div><div class="chessboard">${board()}</div></div><div class="move-list" style="margin-top:14px">${moves.map((m,i)=>`${i%2===0?`<span style="padding:7px;color:var(--muted)">${Math.floor(i/2)+1}.</span>`:''}<button class="${state.selectedMove===i?'active':''}" data-move="${i}">${m}</button>${i%2===0?'':''}`).join('')}</div></div><aside class="rail"><div class="tabs"><button class="active">Branches</button><button>Evidence</button><button>Compare</button></div><div class="branch-tree"><div class="branch"><div class="branch-row"><div><strong>Actual game</strong><br><small>1.e4 c5 2.Nf3 d6 3.d4</small></div>${tag('anchor')}</div><div class="branch active" style="margin-top:8px"><div class="branch-row"><div><strong>What if 3.Bb5+?</strong><br><small>Coach question · 2 replies</small></div>${tag('+0.1','proto')}</div><div class="branch" style="margin-top:8px"><div class="branch-row"><div><strong>...Bd7 4.Bxd7+</strong><br><small>Nested branch · material equal</small></div>${tag('verified')}</div></div></div></div><div class="chat">${state.messages.map(m=>`<div class="msg ${m.who}">${esc(m.text)}</div>`).join('')}</div><form class="composer" id="coach-form"><input id="coach-input" placeholder="Ask why, what if, or compare..." autocomplete="off"><button class="btn primary">Ask</button></form><div class="grid two" style="margin-top:10px"><button class="btn">Create branch</button><button class="btn">Return to anchor</button></div></aside></div></section>`}
function learningView(){const lessons=[['♞','Knight geometry','Jumping, forks and outposts',72],['◩','Saving the correct piece','Defenders, overload and zwischenzug',48],['♙','Pawn breaks','When to open the center',31],['♜','Rook endgames','Activity before pawns',18]];return `<section class="hero"><div class="eyebrow">Learning operating system</div><h2 style="font-size:52px">From explanation to transfer.</h2><p>Courses, puzzles and practice become one adaptive loop: diagnose, explain, rehearse, teach back, then retest later in a changed position.</p><div class="stat-grid"><div class="stat"><b>4</b><span>active learning threads</span></div><div class="stat"><b>11</b><span>misunderstandings captured</span></div><div class="stat"><b>67%</b><span>delayed retry accuracy</span></div><div class="stat"><b>9d</b><span>current learning streak</span></div></div></section><section class="section"><div class="grid two"><div class="card"><div class="section-head"><div><h3>Continue learning</h3><p>Recommendations come from demonstrated errors.</p></div></div><div class="grid">${lessons.map(([ic,t,d,p])=>`<div class="lesson-card"><div class="lesson-icon">${ic}</div><div><strong>${t}</strong><p>${d}</p><div class="progress" style="margin-top:8px"><span style="width:${p}%"></span></div></div><button class="btn">Resume</button></div>`).join('')}</div></div><div class="card"><div class="section-head"><div><h3>Today’s retry</h3><p>Same idea, different position.</p></div></div><div class="callout"><strong>Misunderstanding:</strong> “Pinned pieces cannot defend.”<br><br>The new position tests whether you distinguish legal movement from practical usefulness. Explain your prediction before moving.</div><div class="grid two" style="margin-top:14px"><button class="btn primary">Start retry</button><button class="btn">Review origin</button></div></div></div></section><section class="section"><div class="section-head"><div><h3>Training modes</h3><p>A coherent curriculum, not a shelf of unrelated mini-games.</p></div></div><div class="grid four">${[['Rated puzzles','Pattern calibration'],['Opening practice','Required-line bot'],['Endgames','Challenge + learn'],['Board vision','Coordinates + moves'],['Master games','Play the position'],['Custom drill','Any saved node'],['Teach-back','Explain the idea'],['Puzzle battle','Social speed mode']].map(([a,b],i)=>`<div class="card"><div style="font-size:24px">${['✦','⌁','♙','⊞','♛','◈','◌','⚡'][i]}</div><h4>${a}</h4><p>${b}</p></div>`).join('')}</div></section>`}
function communityView(){return `<section class="hero"><div class="eyebrow">Community, events and identity</div><h2 style="font-size:52px">Chess is a network, not a solo worksheet.</h2><p>Friends, clubs, tournaments, broadcasts and creator studies extend retention, but every social surface carries explicit safety, permission and moderation states.</p></section><section class="section"><div class="grid three"><div class="card"><h4>Live event</h4><p>World Open · Round 7</p><div class="progress" style="margin:18px 0"><span style="width:62%"></span></div><div class="signal"><span class="dot"></span> 14 boards live · commentary on</div></div><div class="card"><h4>Club activity</h4><p>Neurodivergent Chess Learners</p><div class="tag-row">${tag('2 new studies','proto')}${tag('Arena tonight')}</div><button class="btn" style="margin-top:12px">Open club</button></div><div class="card"><h4>Creator study</h4><p>Why the “obvious” recapture loses</p><div class="tag-row">${tag('12 branches')}${tag('4 teach-backs')}</div><button class="btn" style="margin-top:12px">Fork study</button></div></div></section><section class="section"><div class="grid two"><div class="card"><h3>Safety is structural</h3><p style="margin:10px 0 14px">Block state applies to chat, challenges, matchmaking, invitations, tournaments and content. Safe mode removes social interaction without removing chess.</p><div class="grid two"><button class="btn">Privacy controls</button><button class="btn">Enable quiet mode</button></div></div><div class="card"><h3>Roles with sharp edges</h3><p style="margin:10px 0 14px">Club and platform permissions are explicit, scoped and audited. Moderation does not share the same access as fair-play analysis or billing support.</p><div class="tag-row">${tag('Least privilege','p0')}${tag('Audit trail')}${tag('Appeals')}</div></div></div></section>`}
function brdView(){const domains=['All',...new Set(D.requirements.map(x=>x.domain))];const rows=D.requirements.filter(r=>(state.domain==='All'||r.domain===state.domain)&&(state.priority==='All'||r.priority===state.priority)&&(!state.query||`${r.id} ${r.title} ${r.statement} ${r.acceptance}`.toLowerCase().includes(state.query.toLowerCase())));return `<section class="section" style="margin-top:0"><div class="section-head"><div><div class="eyebrow">Business requirements document</div><h3>${D.meta.requirementCount} implementation-grade requirements</h3><p>Filter, search, export, and turn these into delivery slices later.</p></div><a class="btn primary" href="research/requirements.csv" download>Download CSV</a></div><div class="toolbar"><input class="search" id="req-search" value="${esc(state.query)}" placeholder="Search requirement, acceptance, ID..."><select class="select" id="domain-filter">${domains.map(d=>`<option ${state.domain===d?'selected':''}>${esc(d)}</option>`).join('')}</select><select class="select" id="priority-filter">${['All','P0','P1','P2','P3'].map(p=>`<option ${state.priority===p?'selected':''}>${p}</option>`).join('')}</select></div><div class="table-wrap" style="max-height:760px"><table><thead><tr><th>ID</th><th>Domain</th><th>Requirement</th><th>Priority</th><th>Acceptance</th><th>Evidence</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${r.id}</strong><br><small>${r.type}</small></td><td>${esc(r.domain)}</td><td><strong>${esc(r.title)}</strong><br>${esc(r.statement)}</td><td>${tag(r.priority,r.priority.toLowerCase())}<br><small>${esc(r.phase)}</small></td><td>${esc(r.acceptance)}</td><td>${esc(r.sources)}</td></tr>`).join('')}</tbody></table></div><p style="color:var(--muted);font-size:12px">Showing ${rows.length} of ${D.requirements.length} requirements.</p></section>`}
function screensView(){const domains=['All',...new Set(D.screens.map(x=>x.domain))];const rows=state.domain==='All'?D.screens:D.screens.filter(x=>x.domain===state.domain);return `<section class="section" style="margin-top:0"><div class="section-head"><div><div class="eyebrow">Screen and state inventory</div><h3>${D.meta.screenCount} mapped surfaces</h3><p>Granular enough for navigation, design, acceptance tests and analytics ownership.</p></div><a class="btn" href="research/screen-inventory.csv" download>Download CSV</a></div><div class="toolbar"><select class="select" id="domain-filter">${domains.map(d=>`<option ${state.domain===d?'selected':''}>${esc(d)}</option>`).join('')}</select></div><div class="grid three">${rows.map(s=>`<article class="card"><div>${tag(s.id)}${s.status==='Prototype'?tag('Prototype','proto'):tag('Mapped')}</div><h4>${esc(s.screen)}</h4><p>${esc(s.purpose)}</p><div class="tag-row">${tag(s.platform)}${tag(s.auth)}</div></article>`).join('')}</div></section>`}
function architectureView(){const services=[['Edge and clients','Responsive web, native wrappers, accessibility adapters, caching and feature configuration.'],['Identity and policy','Sessions, consent, profile, entitlements, Fair Play Lock and authorization.'],['Play plane','Challenges, matchmaking, tournaments, rooms, clocks, rules and game event log.'],['Learning plane','Review, studies, engine workers, evidence, coach, puzzles, lessons and recommendations.'],['Community plane','Friends, chat, clubs, forums, events, content, notifications and moderation.'],['Data plane','Game archive, position graph, ratings, search, analytics, experiments and exports.'],['Operations plane','Observability, audit, support, billing reconciliation, backups, privacy and incident response.'],['Integration plane','Public API, webhooks, embeds and approved connected-board integrations.']];return `<section class="hero"><div class="eyebrow">Logical architecture</div><h2 style="font-size:52px">Keep the game alive when everything else coughs.</h2><p>The real-time play plane is authoritative and isolated. Coach, content, analytics, chat and commerce may degrade without corrupting an active game or its clocks.</p></section><section class="section"><div class="architecture">${services.map(([a,b])=>`<div class="arch-box"><strong>${a}</strong><small>${b}</small></div>`).join('')}</div></section><section class="section"><div class="grid two"><div class="card"><h3>Evidence-first coach pipeline</h3><ol style="color:var(--muted);line-height:1.8;padding-left:20px"><li>Authorize game context through Fair Play Lock.</li><li>Reconstruct legal history-bearing node.</li><li>Run bounded engine or deterministic facts.</li><li>Build an evidence bundle with version and perspective.</li><li>Generate explanation constrained to the bundle.</li><li>Validate board, arrows, material and cited line.</li><li>Persist coach turn against the node and evidence version.</li></ol></div><div class="card"><h3>Core write discipline</h3><p style="margin-top:10px">Games are append-only ordered events. Studies use revisioned graphs. Analysis is asynchronous and immutable per version. Entitlements and moderation actions are ledgered. Search is permission-filtered before ranking.</p><div class="callout" style="margin-top:16px">Never merge two study nodes merely because the visible board position matches. Repetition, castling, en passant and learner context can differ.</div></div></div></section><section class="section"><div class="section-head"><div><h3>Domain entities</h3><p>Stable names for product, API and data design.</p></div></div><div class="grid four">${D.entities.map(e=>`<div class="card"><h4>${esc(e.entity)}</h4><p>${esc(e.description)}</p></div>`).join('')}</div></section>`}
function researchView(){return `<section class="section" style="margin-top:0"><div class="section-head"><div><div class="eyebrow">Research ledger</div><h3>${D.sources.length} primary sources</h3><p>Publicly indexed first-party documentation and user-owned project records.</p></div><a class="btn" href="research/source-ledger.csv" download>Download ledger</a></div><div class="callout">Chess.com’s current User Agreement restricts automated AI access, systematic copying and competitive-product harvesting. This dossier therefore uses search-indexed public pages, avoids direct automated crawling, and produces original ChessLab screens and assets.</div><div class="source-list" style="margin-top:14px">${D.sources.map(s=>`<div class="source"><strong>${s.id}</strong><div><a href="${s.url}" target="_blank" rel="noreferrer">${esc(s.title)}</a><br><small>${esc(s.publisher)} · ${esc(s.scope)}</small></div><small class="source-date">${esc(s.date)}</small></div>`).join('')}</div></section>`}
function render(){state.domain = ['map','brd','screens'].includes(state.route)?state.domain:'All';let content={overview:overview,map:mapView,prototype:playPrototype,review:reviewView,learning:learningView,community:communityView,brd:brdView,screens:screensView,architecture:architectureView,research:researchView}[state.route]();app.innerHTML=shell(content);bind();}
function bind(){document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>{state.route=b.dataset.route;state.domain='All';state.query='';window.scrollTo(0,0);render()});document.querySelectorAll('[data-domain]').forEach(b=>b.onclick=()=>{state.domain=b.dataset.domain;render()});document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;render()});document.querySelectorAll('[data-move]').forEach(b=>b.onclick=()=>{state.selectedMove=Number(b.dataset.move);render()});document.querySelectorAll('[data-square]').forEach(b=>b.onclick=()=>{const sq=b.dataset.square;if(state.selectedSquare&&state.selectedSquare!==sq&&pieces[state.selectedSquare]){pieces[sq]=pieces[state.selectedSquare];delete pieces[state.selectedSquare];state.selectedSquare=null}else{state.selectedSquare=sq}render()});const form=document.getElementById('coach-form');if(form)form.onsubmit=e=>{e.preventDefault();const input=document.getElementById('coach-input');const q=input.value.trim();if(!q)return;state.messages.push({who:'user',text:q},{who:'ai',text:'Create a branch from this node first. I will compare the legal continuations, material, threats and engine estimate, then explain only what the evidence supports.'});render()};const ds=document.getElementById('domain-filter');if(ds)ds.onchange=e=>{state.domain=e.target.value;render()};const ps=document.getElementById('priority-filter');if(ps)ps.onchange=e=>{state.priority=e.target.value;render()};const rs=document.getElementById('req-search');if(rs)rs.oninput=e=>{state.query=e.target.value;clearTimeout(window.__q);window.__q=setTimeout(render,180)}}
render();
